package com.nu.clubs.clubs_bakend.config;

// Security temporarily disabled per request. When authentication is ready,
// reintroduce Spring Security and wire it to the real user/role store.
public class SecurityConfig {
}
